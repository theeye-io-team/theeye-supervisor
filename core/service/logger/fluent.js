const logger = require('../../lib/logger')(':logger:fluent')
const fluentConfig = require('config').fluent

module.exports = {
  /**
   * @param {String} config customer integration config
   * @param {String} topic 
   * @param {Object} payload 
   */
  submit (config, topic, payload) {
  }
}

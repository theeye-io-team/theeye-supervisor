'use strict'

const debug = require('debug')('eye:entity:resource:template:lifecicle')
//const Template = require('./index').Entity
//const Monitor = require('../../monitor').Entity

module.exports = function (template) {
  //Template.populate(template,() => {
  //  template.monitor_template.remove(err => {
  //    if (err) debug('%o',err)
  //  })
  //})
}
